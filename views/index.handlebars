
<div class="row">
<div class="jumbotron text-center">
  <div class="container-fluid">
    <h1 class="page-title">Real Fake News</h1>
    <h2>Making Fake News Great Again</h2>
    <p>Click the title of the Article to access Biased Liberal Media Safe Zone</p>
    </div>
  </div>
</div>
  <div class="container-fluid">
<div class="row">
    <div class="col-sm-12">
        <div class="col-sm-9">

            {{#each articles}}
              <h3 data-id="{{this._id}}" class="article-title">{{this.title}}</h3>
              <a target="_blank" href="{{this.link}}"><button type="button" class="btn btn-info btn-lg">Read Article</button></a>
              <hr>
            {{/each}}
        </div>
        <div class="col-sm-3">
          <h3>Enter Snowflake Commentary Here</h3>
          <div id="results"></div>
        </div>
    </div>
</div>
  </div>
